<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0
         http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <!--
        Fat-JAR builder — run with:
            mvn install -DskipTests
            mvn package -f pom-fat-jar.xml

        No changes to the original pom.xml required.

        NOTE: the original pom.xml uses spring-boot-maven-plugin with classifier=sut,
        so mvn install produces two JARs:
          - ncs-3.0.0.jar        (thin jar, no classifier)  <used here
          - ncs-3.0.0-sut.jar    (Spring Boot fat jar)      <ignored
        The Shade plugin picks up the thin jar and repackages it with all deps.
    -->

    <groupId>fat-jar-builder</groupId>
    <artifactId>ncs-fat-jar</artifactId>
    <version>1.0.0</version>
    <packaging>jar</packaging>

    <properties>
        <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
        <!-- groupId is inherited from parent in the original pom, but resolved as: -->
        <target.groupId>org.evomaster</target.groupId>
        <target.artifactId>evomaster-benchmark-cs-rest-artificial-ncs</target.artifactId>
        <target.version>4.1.0</target.version>
        <!-- Output JAR name -->
        <fat.jar.name>rest-ncs-impl</fat.jar.name>
    </properties>

    <dependencies>
        <!-- The project to bundle — must be installed first via mvn install -->
        <!-- No classifier here: picks up the thin jar, not the sut fat jar -->
        <dependency>
            <groupId>${target.groupId}</groupId>
            <artifactId>${target.artifactId}</artifactId>
            <version>${target.version}</version>
        </dependency>
    </dependencies>

    <build>
        <!-- No sources to compile — skip compilation entirely -->
        <plugins>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-compiler-plugin</artifactId>
                <version>3.8.1</version>
                <configuration>
                    <skipMain>true</skipMain>
                    <skip>true</skip>
                </configuration>
            </plugin>

            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-shade-plugin</artifactId>
                <version>3.5.1</version>
                <executions>
                    <execution>
                        <phase>package</phase>
                        <goals>
                            <goal>shade</goal>
                        </goals>
                        <configuration>
                            <outputFile>${project.build.directory}/${fat.jar.name}.jar</outputFile>
                            <createDependencyReducedPom>false</createDependencyReducedPom>
                            <shadedArtifactAttached>false</shadedArtifactAttached>

                            <artifactSet>
                                <excludes>
                                    <!-- Provided by the container / test runner -->
                                    <exclude>javax.servlet:javax.servlet-api</exclude>
                                    <!-- Spring Boot launcher — not needed for classpath loading -->
                                    <exclude>org.springframework.boot:spring-boot-loader</exclude>
                                    <!-- Test deps — never needed at runtime -->
                                    <exclude>junit:junit</exclude>
                                    <exclude>org.hamcrest:*</exclude>
                                    <exclude>io.rest-assured:*</exclude>
                                    <exclude>org.codehaus.groovy:*</exclude>
                                    <exclude>org.springframework.boot:spring-boot-starter-test</exclude>
                                </excludes>
                            </artifactSet>

                            <filters>
                                <filter>
                                    <artifact>*:*</artifact>
                                    <excludes>
                                        <!-- Avoid JAR signing conflicts -->
                                        <exclude>META-INF/*.SF</exclude>
                                        <exclude>META-INF/*.DSA</exclude>
                                        <!-- Avoid JPMS conflicts on Java 9+ -->
                                        <exclude>module-info.class</exclude>
                                        <!-- Strip Spring Boot BOOT-INF layout if present -->
                                        <exclude>BOOT-INF/**</exclude>
                                    </excludes>
                                </filter>
                            </filters>
                        </configuration>
                    </execution>
                </executions>
            </plugin>
        </plugins>
    </build>
</project>